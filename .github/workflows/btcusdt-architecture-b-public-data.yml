name: BTCUSDT Architecture B Public Data Baseline

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/btcusdt-architecture-b-public-data.yml"

permissions:
  contents: read

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research/btcusdt-architecture-b-trend-pullback
          fetch-depth: 1
      - name: Validate frozen spec and backtester
        run: |
          test -f docs/BTCUSDT_ARCH_B_FROZEN_BASELINE_SPEC.md
          test -f research/backtest_btcusdt_arch_b.py
          python -m py_compile research/backtest_btcusdt_arch_b.py
      - name: Run frozen BTCUSDT Architecture B baseline
        env:
          BTC_OUT: artifacts/btcusdt-arch-b-baseline
          BTC_CACHE: artifacts/btcusdt-arch-b-baseline/data
        run: python research/backtest_btcusdt_arch_b.py
      - name: Show summary
        run: cat artifacts/btcusdt-arch-b-baseline/summary.csv
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: btcusdt-architecture-b-public-data-${{ github.run_id }}
          path: |
            artifacts/btcusdt-arch-b-baseline/results.json
            artifacts/btcusdt-arch-b-baseline/summary.csv
            artifacts/btcusdt-arch-b-baseline/trades.csv
            artifacts/btcusdt-arch-b-baseline/monthly.csv
            artifacts/btcusdt-arch-b-baseline/equity_curve.csv
          if-no-files-found: error
          retention-days: 30
